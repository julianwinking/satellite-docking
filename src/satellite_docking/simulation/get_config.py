from typing import List


def get_config() -> List[str]:
    "Choose manually which .yaml file to load and test."

    configs = [
        "config_1.yaml",
        "config_2.yaml",
        "config_3.yaml",
    ]

    return configs
